<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/mvc"
			 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
			 xmlns:beans="http://www.springframework.org/schema/beans"
			 xmlns:context="http://www.springframework.org/schema/context"
			 xsi:schemaLocation="http://www.springframework.org/schema/mvc
			 					 http://www.springframework.org/schema/mvc/spring-mvc.xsd
			 					 http://www.springframework.org/schema/beans
			 					 http://www.springframework.org/schema/beans/spring-beans.xsd
			 					 http://www.springframework.org/schema/context
			 					 http://www.springframework.org/schema/context/spring-context.xsd">
	
	<annotation-driven/>
	
	<context:component-scan base-package="com.fpp.controller"></context:component-scan>
	<context:component-scan base-package="com.fpp.service"></context:component-scan>
	<context:component-scan base-package="com.fpp.dao"></context:component-scan>
	
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/"></beans:property>
		<beans:property name="suffix" value=".jsp"></beans:property>
	</beans:bean>
	
	<resources location="/resources/" mapping="/**"></resources>
	
	<!-- <beans:bean id="loginInterceptor" class="com.fpp.interceptor.LoginInterceptor"/> --><!-- interceptor Bean 등록 -->
	
<!-- 	<interceptors>
		<interceptor>
			<mapping path="/admin/**"/>admin으로 들어오는 경우만 mapping 
			exclude-mapping는 옵션
			<beans:ref bean="adminInterceptor" />어떤 경로로 왔을 때 interceptor 기능을 수행할 것인지
		</interceptor>
		
		<interceptor>
			<mapping path="/**"/>모든 경로에 대해서 mapping을 걸겠다
			<exclude-mapping path="/login"/>interceptor로 채가면 오류가 나는 애들이 있어서 예외시킬 애들 지정, interceptor가 기능하지 않을 애들 (ex 로그인이 안 되어 있어도 접근을 허용한다)
			<exclude-mapping path="/signUp"/>
			<exclude-mapping path="/**/*.css"/>기본 리소스 경로*: 모든 경로 **: *의 하위 경로
			<exclude-mapping path="/**/*.js"/>
			<exclude-mapping path="/image/**"/>
			<exclude-mapping path="/main"/>
			<beans:ref bean="loginInterceptor" />
		</interceptor>
	</interceptors> -->
</beans:beans>













