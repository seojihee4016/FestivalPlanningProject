<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board_mapper">
	<!-- 게시판 글 작성 -->
	<insert id="insert">
		INSERT INTO BOARD( BNO , TITLE , CONTENT , WRITER
		) VALUES(
		BOARD_SEQ.NEXTVAL , #{title} , #{content} , #{writer} )
	</insert>

	<!-- 게시판 글 리스트 조회 -->
	<select id="list" resultType="com.fpp.dto.board.BoardDto">
		SELECT BNO , TITLE , WRITER ,
		REGDATE FROM BOARD ORDER BY BNO DESC
	</select>

	<!-- 게시판 글 고유 번호로 조회하기 -->
	<select id="read" parameterType="int"
		resultType="com.fpp.dto.board.BoardDto">
		SELECT BNO , TITLE , CONTENT , WRITER , REGDATE FROM
		BOARD WHERE BNO =
		#{bno}
	</select>

	<!-- 글 수정 -->
	<update id="update" parameterType="com.fpp.dto.board.BoardDto">
		UPDATE BOARD SET TITLE =
		#{title}, CONTENT = #{content} WHERE BNO = #{bno}
	</update>

	<!-- 글 삭제 -->
	<delete id="delete" parameterType="int"> DELETE FROM BOARD WHERE
		BNO = #{bno}
	</delete>
</mapper>